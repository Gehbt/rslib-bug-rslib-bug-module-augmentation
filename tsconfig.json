{
  "compilerOptions": {
    "lib": [
      "ESNext",
      // when has DOM, the generated dts is broken.
      "DOM"
    ],
    "paths": {
      "#lib": ["${configDir}/src/lib.ts"]
    }
  },
  "include": [
    "src/**/*.ts"
  ],
  "references": []
}